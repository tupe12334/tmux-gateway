---
displayNumber: 11
status: open
priority: 2
createdAt: 2026-03-14T12:30:22.806751+00:00
updatedAt: 2026-03-14T12:30:22.806751+00:00
---

# Fix Dockerfile: restore protobuf-compiler and improve cache invalidation

The Dockerfile has an uncommitted change that removes protobuf-compiler installation and changes cache invalidation strategy. These changes need to be reviewed and properly committed.

## Current Issues
1. protobuf-compiler removal: The RUN apt-get line installing protobuf-compiler was removed. Since the project uses protox (pure Rust proto compiler) this may be intentional, but needs verification that the Docker build still works end-to-end.
2. Cache invalidation: Changed from 'touch src/main.rs' to 'find . -name *.rs -exec touch {} +' which is more aggressive but ensures all source files trigger recompilation.

## Action Items
- Verify Docker build succeeds without protobuf-compiler
- If protox handles everything at runtime, the removal is correct - commit it
- If not, restore the apt-get install line
- Test the full Docker image: build, run, and verify all 3 APIs work
- Document the decision in a commit message
