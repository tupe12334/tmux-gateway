---
displayNumber: 34
status: closed
priority: 3
createdAt: 2026-03-14T13:15:58.838884+00:00
updatedAt: 2026-03-14T15:09:02.515675+00:00
---

# Make schema export optional in production via env var

export_schemas::export_all() runs on every server startup (src/main.rs line 24), writing 4 files to the schemas/ directory. In production this is unnecessary disk I/O and could fail if the directory isn't writable.

## Problem:
- Schema files are only useful for development and CI drift detection
- Production containers may have read-only filesystems
- Adds startup latency (proto compilation + file writes)
- Uses .expect() which will panic if writes fail (related to #23)

## Fix:
- Gate schema export behind an env var: EXPORT_SCHEMAS=true (default false in production)
- Or only export when schemas/ directory exists and is writable
- Consider moving schema export to a separate binary/subcommand (already exists as src/bin/export_schemas.rs)
